# TODO

## Admin Login Gate (server-side)
- [ ] Create admin login page (`calendar-monitoring/admin.html`)
- [ ] Add session middleware and auth routes in `calendar-monitoring/server.js`
- [ ] Protect `/` and `/api/requests*` when not authenticated
- [ ] Update `calendar-monitoring/package.json` with `express-session`
- [ ] Install dependency + run server, verify redirect and API protection

